import { Contact } from "@/components/sections/contact";

export const metadata = {
  title: "Contact — Andrew Bieh-Mintah",
};

export default function ContactPage() {
  return (
    <div className="pt-20">
      <Contact />
    </div>
  );
}
